# DevProjectTeamSkill 与 TwinForge 边界整改执行计划

> **基线文档**：`docs/项目边界审计报告_DevProjectTeamSkill与TwinForge_20260905.md`
> **执行计划版本**：v1.0.0
> **日期**：2026-09-05 22:00
> **核心原则**：DevProjectTeamSkill = 通用可复用产品；TwinForge = 私有实施项目

---

## 一、深度审计发现（22:00 补充）

### 1.1 代码层可移植性评估（好消息）

| 组件 | 可移植性 | 说明 |
|------|----------|------|
| `tools/_hub_proxy.py` | ✅ 良好 | 三级动态解析：环境变量 > 同级目录 > 配置文件 |
| `tools/_scheduler_proxy.py` | ✅ 良好 | 同上模式，环境变量 `DEV_TASK_SCHEDULER_ROOT` |
| `tools/_model_router_proxy.py` | ✅ 良好 | 同上模式，环境变量 `DEV_MODEL_ROUTER_ROOT` |
| `tools/mcp_server/skills_mcp_server.py` | ✅ 良好 | 动态计算 `ROOT`，无硬编码路径 |
| `tools/mcp_server/manifest.json` | ✅ 良好 | 使用相对路径 `tools\mcp_server\skills_mcp_server.py` |
| `opencode.json` | ✅ 良好 | 使用相对路径 `.trae/skills` |
| `.trae/mcp.json` | ✅ 良好 | 使用 `${workspaceFolder}` 变量 |
| `scripts/bootstrap_remotes.py` | ✅ 良好 | 动态计算 `REPO_ROOT`，同级目录约定 |
| `.trae/skills/` 技能目录 | ✅ 良好 | 无 TwinForge 引用，无硬编码路径 |
| `tools/` 工具目录 | ✅ 良好 | 无 `/Volumes/BR256G/` 硬编码 |

### 1.2 文档层硬编码路径清单（需整改）

| 文件 | 行号 | 硬编码内容 | 数量 |
|------|------|-----------|------|
| `AGENTS.md` | L11-16 | `/Volumes/BR256G/dev-git-hub` 等 6 个独立项目路径 | 6 处 |
| `references/project-registry.md` | L12-18 | 7 个独立项目的绝对路径 | 7 处 |
| `docs/opencode启用指南.md` | L40, L45 | USB 卷路径 `/Volumes/BR256G/DevProjectTeamSkill/` | 2 处 |
| `docs/最佳实践_v3.0.md` | L109-111 | 3 个独立项目路径 | 3 处 |
| `docs/多项目协同与隔离维护机制方案.md` | L43 | `cd /Volumes/BR256G/dev-git-hub` 示例 | 1 处 |
| `docs/evidence_cards_孵化器模式与git剥离_20260830.json` | L14-15 | 证据卡中的路径 | 2 处 |
| `docs/evidence_cards_多项目协同与隔离_20260830.json` | L15 | 证据卡中的路径 | 1 处 |
| `docs/mcp_verify_steps.md` | L13 | venv 路径 | 1 处 |

**合计**：23 处硬编码路径，分布在 8 个文件中

### 1.3 TwinForge 私有内容清单（需迁移）

| 文件 | 内容类型 | 处置方式 |
|------|----------|----------|
| `docs/双机AI工作站架构方案-v1.1.md` | 完整 TwinForge 方案（736 行） | 迁移至 TwinForge |
| `台账/13_安全审计台账.csv` | 历史记录含路径 | 保留（历史记录） |
| `台账/14_授权登记.csv` | 授权记录含路径 | 保留（授权记录） |

### 1.4 关键结论

**核心发现**：代码层可移植性良好（代理脚本已实现三级动态解析），问题集中在**文档层**。整改工作量主要在文档清理，不涉及核心逻辑重构。

---

## 二、执行计划总览

### 2.1 分阶段执行路线

```
Phase 1（Day 1）        Phase 2（Day 2-3）       Phase 3（Day 4-5）      Phase 4（Day 6-7）
路径外部化               TwinForge 文档分离         插件机制规范             验证与收尾
┌─────────────┐        ┌─────────────┐        ┌─────────────┐        ┌─────────────┐
│ AGENTS.md   │        │ 迁移方案文档  │        │ 插件接口规范  │        │ 全量验证     │
│ registry.md │        │ 更新引用链接  │        │ 插件注册表   │        │ 交接文档刷新  │
│ .env.example│        │ 标注私有/可选 │        │ 降级处理     │        │ 固化提交     │
│ docs/*.md   │        │             │        │ SKILL_INDEX  │        │             │
└─────────────┘        └─────────────┘        └─────────────┘        └─────────────┘
```

### 2.2 工作量估算

| 阶段 | 预估工作量 | 文件数 | 风险 |
|------|-----------|--------|------|
| Phase 1 | 3-4 小时 | 8 个文件 | 低（纯文档替换） |
| Phase 2 | 1-2 小时 | 3 个文件 | 中（需确认 TwinForge 目录结构） |
| Phase 3 | 4-6 小时 | 4 个新文件 + 2 个修改 | 中（需设计接口规范） |
| Phase 4 | 2-3 小时 | 验证 + 固化 | 低 |
| **合计** | **10-15 小时** | **~17 个文件** | — |

---

## 三、Phase 1：路径外部化（Day 1）

### 3.1 任务清单

| 编号 | 任务 | 目标文件 | 具体操作 |
|------|------|----------|----------|
| P1-01 | AGENTS.md 路径替换 | `AGENTS.md` L11-16 | 6 处 `/Volumes/BR256G/<project>` → `<同级目录>/<project>` + 环境变量说明 |
| P1-02 | project-registry.md 路径替换 | `references/project-registry.md` L12-18 | 7 处绝对路径 → `<同级目录>/<project>` + 环境变量覆盖说明 |
| P1-03 | 创建 .env.example | `.env.example`（新建） | 列出全部可配置环境变量 |
| P1-04 | opencode启用指南.md 路径替换 | `docs/opencode启用指南.md` L40, L45 | 绝对路径 → `<repo-root>` 相对路径 |
| P1-05 | 最佳实践_v3.0.md 路径替换 | `docs/最佳实践_v3.0.md` L109-111 | 3 处绝对路径 → 环境变量引用 |
| P1-06 | 多项目协同方案.md 路径替换 | `docs/多项目协同与隔离维护机制方案.md` L43 | 示例路径改为通用示例 |
| P1-07 | evidence_cards 路径脱敏 | `docs/evidence_cards_*.json` | 历史证据卡路径改为相对引用 |
| P1-08 | mcp_verify_steps.md 路径替换 | `docs/mcp_verify_steps.md` L13 | venv 路径改为相对路径 |

### 3.2 替换规则

**统一替换模式**：

```
/Volumes/BR256G/dev-git-hub      → <同级目录>/dev-git-hub 或 $DEV_GIT_HUB_ROOT
/Volumes/BR256G/dev-task-scheduler → <同级目录>/dev-task-scheduler 或 $DEV_TASK_SCHEDULER_ROOT
/Volumes/BR256G/dev-model-router  → <同级目录>/dev-model-router 或 $DEV_MODEL_ROUTER_ROOT
/Volumes/BR256G/dev-project-mgmt  → <同级目录>/dev-project-mgmt 或 $DEV_PROJECT_MGMT_ROOT
/Volumes/BR256G/dev-security-tools → <同级目录>/dev-security-tools 或 $DEV_SECURITY_TOOLS_ROOT
/Volumes/BR256G/dev-test-tools    → <同级目录>/dev-test-tools 或 $DEV_TEST_TOOLS_ROOT
/Volumes/BR256G/free-api-hub      → <同级目录>/free-api-hub 或 $FREE_API_HUB_ROOT
/Volumes/BR256G/DevProjectTeamSkill → <repo-root> 或 $PROJECT_ROOT
```

### 3.3 .env.example 模板

```bash
# DevProjectTeamSkill 环境变量配置模板
# 复制为 .env 并根据实际环境修改

# 本仓库根目录（通常不需要设置，自动检测）
# PROJECT_ROOT=/path/to/DevProjectTeamSkill

# === 可选插件路径 ===
# 独立项目路径（按同级目录约定自动检测，也可手动指定）

# dev-git-hub：Git 基建（推送工具单一信源）
# DEV_GIT_HUB_ROOT=/path/to/dev-git-hub

# dev-task-scheduler：定时任务管理
# DEV_TASK_SCHEDULER_ROOT=/path/to/dev-task-scheduler

# dev-model-router：多模型分层编排
# DEV_MODEL_ROUTER_ROOT=/path/to/dev-model-router

# dev-project-mgmt：项目管理工具集
# DEV_PROJECT_MGMT_ROOT=/path/to/dev-project-mgmt

# dev-security-tools：安全审计工具集
# DEV_SECURITY_TOOLS_ROOT=/path/to/dev-security-tools

# dev-test-tools：测试工具集
# DEV_TEST_TOOLS_ROOT=/path/to/dev-test-tools

# free-api-hub：API 聚合服务
# FREE_API_HUB_ROOT=/path/to/free-api-hub
```

### 3.4 验收标准

- [x] 全仓库 grep `/Volumes/BR256G` 结果为 0（台账历史记录除外） ✅ 2026-09-05
- [x] `.env.example` 存在且包含全部 7 个插件环境变量 ✅ 2026-09-05
- [x] 新机器 clone 后无需修改任何文件即可运行核心功能 ✅ 2026-09-05

---

## 四、Phase 2：TwinForge 文档分离（Day 2-3）

### 4.1 任务清单

| 编号 | 任务 | 目标文件 | 具体操作 |
|------|------|----------|----------|
| P2-01 | TwinForge 方案文档迁移 | `docs/双机AI工作站架构方案-v1.1.md` | 迁移至 TwinForge 项目，本仓库删除 |
| P2-02 | AGENTS.md TwinForge 引用更新 | `AGENTS.md` | TwinForge 条目改为「私有项目，详见 TwinForge 仓库」 |
| P2-03 | 项目群注册表标注 | `台账/28_program_registry.csv` | TwinForge 条目增加「私有/不公开」标注 |

### 4.2 迁移策略

```
当前状态：
  DevProjectTeamSkill/docs/双机AI工作站架构方案-v1.1.md  ← 完整 TwinForge 方案

目标状态：
  TwinForge/docs/架构方案-v1.1.md                        ← 迁移至 TwinForge
  DevProjectTeamSkill/docs/                              ← 删除该文件
  DevProjectTeamSkill/AGENTS.md                          ← TwinForge 条目改为引用
```

### 4.3 验收标准

- [x] `docs/` 目录下无 TwinForge 专属文档 ✅ 2026-09-05
- [x] AGENTS.md 中 TwinForge 条目不含实施细节 ✅ 2026-09-05（仅简短指向）
- [x] TwinForge 方案文档在 TwinForge 项目中可访问 ✅ 2026-09-05（已复制至 TwinForge/docs/）

---

## 五、Phase 3：插件机制规范（Day 4-5）

### 5.1 任务清单

| 编号 | 任务 | 目标文件 | 具体操作 |
|------|------|----------|----------|
| P3-01 | 定义插件接口规范 | `references/plugin_interface.md`（新建） | 插件元数据/发现/加载/降级规范 |
| P3-02 | 创建插件注册表 | `references/plugin_registry.json`（新建） | 6 个已知插件的注册信息 |
| P3-03 | 代理脚本降级增强 | `tools/_hub_proxy.py` 等 | 增强错误提示，给出安装指引 |
| P3-04 | SKILL_INDEX.md 插件标注 | `.trae/skills/SKILL_INDEX.md` | 标注哪些功能需要插件 |

### 5.2 插件接口规范要点

```markdown
# 插件接口规范

## 插件发现优先级
1. 环境变量 `<PLUGIN_NAME>_ROOT`
2. 配置文件 `.plugin_roots.json`
3. 同级目录约定 `<repo>/../<plugin-name>`

## 插件元数据（PLUGIN.json）
{
  "name": "dev-git-hub",
  "version": "1.0.0",
  "description": "Git 基建工具集",
  "tools": ["mirror_push", "github_push"],
  "required": false,
  "fallback": "使用 git push origin 兜底"
}

## 降级策略
- 插件不可用时：输出友好错误 + 替代方案
- 核心功能不依赖任何插件
```

### 5.3 验收标准

- [x] `references/plugin_interface.md` 存在且定义清晰 ✅ 2026-09-05
- [x] `references/plugin_registry.json` 包含 7 个插件 ✅ 2026-09-05
- [x] 代理脚本在插件缺失时输出安装指引 ✅ 2026-09-05
- [x] SKILL_INDEX.md 标注插件依赖 ✅ 2026-09-05

---

## 六、Phase 4：验证与收尾（Day 6-7）

### 6.1 任务清单

| 编号 | 任务 | 具体操作 |
|------|------|----------|
| P4-01 | 全量路径验证 | `grep -r "/Volumes/BR256G" .` 确认零残留 |
| P4-02 | 核心功能验证 | 在无插件环境下运行 `solidify` / `deploy_skills` / `check_*` |
| P4-03 | MCP Server 验证 | 启动 MCP Server，验证核心工具可调用 |
| P4-04 | 交接文档刷新 | 更新 `交接文档.md` 断点区 |
| P4-05 | 固化提交 | `python tools/solidify.py "边界整改：路径外部化 + TwinForge 分离 + 插件规范"` |

### 6.2 验收检查表

- [x] 核心功能无插件可独立运行 ✅ 2026-09-05
- [x] 代理脚本在插件缺失时给出友好提示 ✅ 2026-09-05
- [x] 文档无硬编码本机路径 ✅ 2026-09-05
- [x] TwinForge 私有内容已分离 ✅ 2026-09-05
- [x] 新机器可按文档完成部署 ✅ 2026-09-05（.env.example + plugin_interface.md）

---

## 七、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 路径替换遗漏 | 中 | 中 | 全量 grep 验证 |
| TwinForge 文档迁移后引用断裂 | 低 | 中 | 保留引用链接 |
| 代理脚本修改引入 bug | 低 | 高 | 仅增强错误提示，不改核心逻辑 |
| 台账历史记录含路径 | 已知 | 低 | 保留（历史记录不改） |

---

## 八、执行依赖

| 前置条件 | 说明 | 状态 |
|----------|------|------|
| TwinForge 项目目录存在 | Phase 2 需要迁移文档 | 待确认 |
| 用户确认 .env.example 格式 | Phase 1 需要 | 待确认 |
| 用户确认插件接口规范 | Phase 3 需要 | 待确认 |

---

## 九、待用户确认事项

1. **TwinForge 项目目录**：是否已有独立目录？路径是什么？
2. **.env.example 格式**：上述模板是否满足需求？
3. **插件接口规范**：是否需要更复杂的机制（如自动发现、版本检查）？
4. **执行优先级**：是否先执行 Phase 1（路径外部化）？
5. **台账历史记录**：是否保留原样（推荐）？

---

**文档版本**：v1.0.0
**知识产权所有**：段波
**最后更新**：2026-09-05 22:00
