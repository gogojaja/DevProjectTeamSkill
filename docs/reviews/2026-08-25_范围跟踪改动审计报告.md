# 范围跟踪机制改动审计报告

> 审计方式：self-improve 技能优化模式（OODA 闭环：Observe→Orient→Decide→Act）
> 审计时间：2026-08-25 18:22~18:45
> 审计对象：本会话"完善项目范围跟踪机制"全部改动（commit `aa8bbab`，11 文件）
> 审计人：WorkBuddy（AI 技能库维护视角）

---

## 1. 审计范围与证据

| 类别 | 对象 | 证据 |
|------|------|------|
| 标准 | `.trae/skills/references/traceability_standard.md` v1.1.0 | 全文读取 |
| 技能 | `.trae/skills/role-governance/domain/scope-change.md` v21.1.0 | 全文读取 |
| 工具 | `tools/scope_tracker.py`（init/metrics/gate/change） | 全文读取 + 实测 metrics/gate 通过 |
| 数据 | `台账/需求-架构-代码追溯矩阵.csv`（扩 6 列，11 行）、`06/07_范围台账.csv` | 表头/数据读取 |
| 契约 | `SKILL_INDEX.md`、`references/api_contracts.md`、`role-governance/SKILL.md` | grep 比对 |
| 门禁 | `tools/check_skill_links.py`、`check_traceability.py`、`.gitignore`、git 跟踪状态 | 实测/检查 |
| 部署 | 全局 `~/.workbuddy/skills/` 3 文件 | diff 一致（SAME） |

---

## 2. 偏差清单（Deviation Log）

| ID | 严重度 | 描述 | 预期（标准/铁律） | 实际 | 影响 |
|----|--------|------|------------------|------|------|
| DEV-01 | **高** | `台账/07_范围跟踪台账.csv` 被 git 跟踪并提交（aa8bbab） | AGENTS.md 铁律 #12：脚本每次运行自动追加的台账类产物必须 `git rm --cached` + `.gitignore`（同 `32_镜像同步记录.csv` 先例） | 07 台账由 `metrics --write`/`gate` 每次运行 append 快照行，已入库 | 每次跑门禁都产生未提交改动 → 反复 commit/push 死循环（DEV-002 事故模式重演） |
| DEV-02 | 中高 | `台账/13_安全审计台账.csv` 含 B 级本机路径未脱敏入库 | 铁律 #8：用户名/绝对路径提交公共仓库前必须脱敏 | OP-AUDIT-034~037 均含 `C:\Users\gogoj\...`、`D:\MyProjects\...` 完整路径（含已提交内容） | 公共仓库泄露本机用户名与目录拓扑（B 级信息） |
| DEV-03 | 中 | 契约未同步：`references/api_contracts.md` 无 `scope_tracker.py` 工具契约；`role-governance/SKILL.md` 未提及新工具/新标准 | 铁律 #3：新增/修改技能必须同步 `SKILL_INDEX.md` + `references/api_contracts.md` | api_contracts 仅登记 action 级 `change_audit/check_gate`，工具层契约缺失；包根 SKILL.md 无 `scope_tracker/traceability_standard` 引用 | 消费端按文档调用工具时无契约可依；包内依赖清单不完整 |
| DEV-04 | 中 | `gate` 快照的 `GATE_RESULT` 列恒为"指标快照"，门禁结论（通过/警告/驳回）未留痕 | 标准 §3.2/§8：07 台账快照应承载门禁结论 | `_write_snapshot()` 第 16 列硬编码 `'指标快照'`（代码 L220-236 + 07 台账实测 3 行均为该值） | 门禁结果无审计留痕，事后无法追溯"何时通过" |
| DEV-05 | 中 | 健康分未接入 `gate` 判定 | 标准 §8：范围健康分 ≥ 90 门禁通过 | `cmd_gate` 仅打印 health，severe/warn 判定只用 violations/creep/shrink | 标准承诺的"健康分门禁"未落地，门禁可"高分驳回、低分通过" |
| DEV-06 | 中 | 一致性校验异常时 `gate` 静默降级（假绿风险） | 门禁应 fail-closed | `consistency_violations()` try/except 打印警告后返回 `[]`，gate 继续判定通过 | 若 `check_traceability.py` 异常，门禁误报"通过"，流转失控 |
| DEV-07 | 中低 | 蔓延检测与标准 §6.2 承诺不完全一致 | 标准：检测"未关联已审批 CR 的新 AE/MOD/TC" + `Won't` 已实现 + 基线对比 | 实现仅：AE 孤儿 + `Won't` 已实现；MOD/TC 孤儿未查、CR 关联未校验、无基线版本对比 | 部分蔓延场景漏检（gold-plating 的代码/测试悬空） |
| DEV-08 | 低 | 07 台账 `AE_TOTAL/MOD_TOTAL/TC_TOTAL` 口径为"含该列的行数"而非去重元素总数 | 列名语义应为元素总数 | `_write_snapshot()` 用 `len([r for r in rows if split_ids(...)])` | 快照指标误导（本库当前 11 行×满值，恰好等于元素数，掩盖问题） |
| DEV-09 | 低 | 既有 7 处断链未清理 | 技能库引用可达性（check_skill_links 全绿） | `role-project-init/domain/project-init.md` → `项目铁律.md` 7 处不可达（历史遗留，非本轮引入） | 与 SKILL_INDEX"v21.8.2 lint 全绿"声明不符；铁律 #9 关联清理未做 |
| DEV-10 | 低 | RTM 数据行 ID 与标准 ID Scheme 不一致 | 标准 §3.1：`REQ-<nnn>`（REQ-001） | 既有 11 行数据用 `REQ-01`（两位，历史遗留）；init 示例用 `REQ-001`（三位） | 标识符规范与存量数据漂移，追溯引用易混 |
| DEV-11 | 低 | `tools/scope_tracker.py` 无单元测试 | 本库工具质量要求（tests/ 已有 6 个用例） | 无 `tests/test_scope_tracker.py` | 门禁/蔓延缩水逻辑回归无保障 |

**正面确认（无偏差项）**：
- ✅ 版本一致性：标准 v1.1.0 ↔ scope-change v21.1.0 ↔ 工具头注释 v1.1.0，互相引用一致；
- ✅ 全局部署一致性：3 个同步文件与仓库源 diff 全 SAME；
- ✅ `check_traceability.py` 复用契约正确（`analyze` 返回 `(violations, stats)` 二元组，调用匹配）；
- ✅ 本轮未引入新断链（check_skill_links 仅报既有 7 处）；
- ✅ solidify 4 硬门禁通过、dist 10 包含改进、项目级三目录公共层已同步；
- ✅ RTM 表头与 `RTM_COLS`（5 基础 + 6 扩展 = 11 列）完全一致；06 台账表头与 `CHANGE_COLS` 一致。

---

## 3. 根因分析（Orient）

| 偏差 | 5-Why 根因链 | 根因归类 |
|------|-------------|----------|
| DEV-01 | 为何 07 台账入库？→ 把"工具自动追加快照"误当"人工审计台账"处理 → 编写工具时未对照铁律 #12 清单 → 交付清单缺"输出产物 git 跟踪状态检查" | 流程 |
| DEV-02 | 为何路径入库？→ 台账惯例直接写本机路径 → 提交前未执行铁律 #8"自问属于哪级" → 提交清单无脱敏检查项 | 流程 |
| DEV-03 | 为何契约未同步？→ 新增工具/标准后未跑"铁律 #3 同步清单" → 改技能 SOP 未把"新工具登记 api_contracts + 包根 SKILL.md 依赖"列为必做步骤 | 流程/标准 |
| DEV-04/05/06 | 为何 gate 语义缺漏？→ 工具一次成型未走 review → 标准文本承诺与实现逐条核对缺失 → 无测试兜底（DEV-11 同源） | 工具/流程 |
| DEV-07 | 为何蔓延检测不全？→ v1.0.0 采用启发式先行 → 基线对比/CR 关联需快照数据模型，未纳入首发 | 工具（有意裁剪，需显式声明） |
| DEV-09 | 为何断链残留？→ 历史遗留未纳入本轮范围 → 无例行 lint 巡检机制 | 环境/流程 |

**根因汇总**：主要根因是**交付清单缺失**——新增工具/标准后，未执行"铁律 #3 契约同步、铁律 #8 脱敏自问、铁律 #12 自变产物 gitignore、工具 review + 测试"四项收尾检查。次要根因是 gate 语义实现与标准文本未逐条对齐。

---

## 4. 改进提案（Decide）

### P1 — 必修（合规修正，需用户确认后执行）
| 提案 | 对应偏差 | 方案 | 成本/风险 |
|------|----------|------|-----------|
| P1-1 | DEV-01 | `git rm --cached 台账/07_范围跟踪台账.csv` + `.gitignore` 追加 `台账/07_范围跟踪台账.csv`（对齐 32 先例），新增修正 commit | 低/极低 |
| P1-2 | DEV-02 | 13 台账 OP-AUDIT-034~037 本机路径脱敏（`C:\Users\gogoj\...`→`C:\Users\<user>\...`、保留项目内相对路径），新增修正 commit | 低/极低 |
| P1-3 | DEV-03 | `api_contracts.md` §1.2 补 `scope_tracker` 工具契约（4 子命令 + 退出码）；`role-governance/SKILL.md` 依赖清单补 `tools/scope_tracker.py` + `references/traceability_standard.md` | 低/低 |

### P2 — 工具加固（下次技能迭代版本化发布）
| 提案 | 对应偏差 | 方案 | 优先级 |
|------|----------|------|--------|
| P2-1 | DEV-04 | `_write_snapshot` 增 `gate_result` 参数，`cmd_gate` 传入实际结论写入 `GATE_RESULT` 列 | P1 收益 |
| P2-2 | DEV-05 | `cmd_gate` 增加 `--min-health 90`（默认 90），health < 阈值 → `驳回`，与标准 §8 对齐 | P1 收益 |
| P2-3 | DEV-06 | `consistency_violations` 异常时 `gate` fail-closed（返回 exit 2 + 驳回），仅 `metrics` 保留降级 | P1 收益 |
| P2-4 | DEV-07 | 蔓延检测补 MOD/TC 孤儿；CR 关联校验（`CHANGE_REFS` 空但状态推进 → 提示） | P2 收益 |
| P2-5 | DEV-08/10 | `AE_TOTAL` 等改去重元素数；RTM 存量 `REQ-01`→`REQ-001` 一次性迁移（或标准明确兼容规则） | P3 收益 |

### P3 — 质量收尾
| 提案 | 对应偏差 | 方案 |
|------|----------|------|
| P3-1 | DEV-11 | 新增 `tests/test_scope_tracker.py`（示例 RTM 上跑 metrics/gate/change 冒烟 + 蔓延缩水用例） |
| P3-2 | DEV-09 | 修复 `role-project-init/domain/project-init.md` 7 处 `项目铁律.md` 断链（正确相对路径），使 lint 全绿 |

---

## 5. 结论

1. **机制设计正确**：标准-技能-工具-数据四层闭环成立，版本/全局/复用契约一致，solidify 门禁通过——**范围跟踪机制本体健全**。
2. **交付收尾存在合规缺口**：DEV-01（07 台账入库，高）违反铁律 #12，需立即修正；DEV-02（B 级路径未脱敏）、DEV-03（契约未同步）为铁律 #8/#3 的流程缺失，需本轮补齐。
3. **工具存在 4 处语义缺陷**（DEV-04~07）：门禁结论不留痕、健康分未接入、异常假绿、蔓延漏检——建议下一技能版本（v1.1.1）随 P2 加固发布。
4. **后续动作**：P1 三项需用户确认后执行（涉及 git 历史修正与技能源修改）；P2/P3 纳入下次技能迭代，走 solidify 版本化发布。

---

**审计人**：WorkBuddy（self-improve v1.1.0 流程）
**审计结论**：机制健全，收尾待修（P1×3 必修 / P2×5 加固 / P3×2 质量）
