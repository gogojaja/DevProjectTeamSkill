# 投产固化能力改进执行计划

> **关联方案**：`docs/投产固化能力改进方案_20260905.md`（v1.1.0 评审吸收版）
> **评审报告**：`docs/评审报告_投产固化能力改进_20260905_多视角评审.csv`
> **流程状态**：✅ Phase 1+2 全部完成，已固化提交（`00a5a4c`）并推送三端远端

---

## Phase 1：门禁工具开发（G-01 + G-02）

### 任务清单

| 编号 | 任务 | 目标文件 | 具体操作 | 工作量 |
|------|------|----------|----------|:------:|
| P1-01 | 新建 MCP Server 门禁工具 | `tools/check_mcp_server.py` | py_compile 语法检查 + @mcp.tool() 计数(≥10) + 工具引用路径可达性 + requirements.txt 检查 | 45min |
| P1-02 | 新建插件链路检查工具 | `tools/check_plugin_chain.py` | 代理脚本编译检查 + plugin_registry.json 有效性 + .env.example 存在性 + proxy_module 引用验证 | 30min |
| P1-03 | 集成到 solidify.py | `tools/solidify.py` | 新增 [1e/6] MCP Server 硬门禁 + [1f/6] 插件链路软门禁（标注「不阻断」） | 15min |
| P1-04 | 回归验证 | — | 跑一次完整 solidify，确认 6 步全部通过 | 15min |

### 验收标准

- [x] `python tools/check_mcp_server.py` 独立运行成功
- [x] `python tools/check_plugin_chain.py` 独立运行成功
- [x] `solidify.py` 输出包含 [1e/6] 和 [1f/6] 步骤
- [x] 现有 4 硬门禁不受影响（回归通过）

---

## Phase 2：消费端验证 + 回滚（G-04 方案 A）

### 任务清单

| 编号 | 任务 | 目标文件 | 具体操作 | 工作量 |
|------|------|----------|----------|:------:|
| P2-01 | publish_production 增加 --verify | `tools/publish_production.py` | 末尾增加消费端验证：全局库文件数 ≥ 预期 + SKILL_INDEX.md 存在 | 20min |
| P2-02 | 回滚指针 | `tools/publish_production.py` | 发布前记录 current 软链目标到 .backup/last_production_version.txt | 10min |

### 验收标准

- [x] `python tools/publish_production.py --dry-run --verify` 成功
- [x] `.backup/last_production_version.txt` 记录上一版本号（实际发布时写入）

---

## Phase 3：CI/CD 流水线（G-03，后续优化）

> **评审建议 R-03**：当前单人维护阶段暂缓，待多人协作阶段实施。

**状态**：🔒 暂缓（后续优化）

---

## 执行顺序

```
P1-01 → P1-02 → P1-03 → P1-04（回归验证）→ P2-01 → P2-02 → 固化提交
```

**预计总工作量**：~2.5 小时（Phase 1+2，Phase 3 暂缓）

---

**文档版本**：v1.0.0
**知识产权所有**：段波
**最后更新**：2026-09-05
