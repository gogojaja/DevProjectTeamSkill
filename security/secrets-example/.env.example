# 密钥/凭据模板（占位符，禁止填真实值）

> 本目录**仅存放占位符模板**，供开发/测试/部署各环境引用格式参考。
> **真实账号与密钥禁止写入本仓库**（`.gitignore` 已排除 `secrets/`、`*.env`、`*.pem`、`*.key` 等）。
> 本地真实密钥请放置于项目外 `.secrets/`（已被 `.gitignore` 排除，且建议用本机凭据管理器/DPAPI 加密），
> 通过环境变量名或密钥引用（如 `${API_KEY_ALIAS}`）在技能流程中引用，禁止明文。

---

## 1. 环境配置引用示例（.env）

```env
# ── 复制为 .env ──（.env 已被 .gitignore 排除，不入库）──
# 域名 / 连接串
DEV_HOST=******
TEST_HOST=******
PROD_HOST=******

# 账号（仅占位）
DB_USER=******
API_USER=******

# 密钥：本地经凭据管理器/加密 .env 注入，禁止明文落在本仓库
DB_PASSWORD_ALIAS=db_pass_dev
API_KEY_ALIAS=feishu_api_key
```

## 密钥安全规则（引用方式）

1. **仓库内禁止出现真实密钥明文**：只允许 `占位符`（如 `******`）或 `别名`（如 `API_KEY_ALIAS`）；
2. **本地真实密钥**存放于项目 `.secrets/`（git ignored）或本机凭据管理器，经加密后通过脚本按别名替换；
3. **切环境**用 `20_环境配置.csv` 引用别名，不写明文；
4. **泄露即轮换**密钥；提交前经 pre-commit 密钥扫描。

---

**文件版本**：v21.3.0　**创建日期**：2026-08-07