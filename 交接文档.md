# 交接文档

## 0. 速览

- **项目目标**：DevProjectTeamSkill 软件研发多角色编排技能库（9 角色包 + 1 编排器）的维护与增强——Token/CSV 化改造、单源共享、敏捷迭代模式、模型选型/阶段复盘/环境抽取、审计授权铁律、目录访问边界、弱模型适配、启动治理完善、项目群/项目集协同层等已全部落地（v21.6.0）。
- **当前阶段**：#11 收口完成；v21.3.x~v21.6.x 增强（模型选型/复盘/环境/铁律卡/审计授权/访问边界/弱模型适配/启动治理/项目群协同）已固化入 git；最新为 v21.6.0（新增项目群/项目集协同层 role-program-mgmt：7 环节 + 3 台账 + 治理三层模型 + Program Board tranche 决策）。
- **下一步唯一动作**：按用户指令推进下一轮技能库维护或进入具体项目执行；文档整理批次已固化。

## 1. 工作断点

> 本区由 `tools/solidify.sh` 每次任务完成后自动覆写。
> **新模型/新会话启动，第一步必须先读 `交接文档.md` 全文**，从本区定位上一模型已完成/待办，未读交接文档前禁止读其他项目文档。

**最近固化时间**：2026-08-17 21:10:06
**角色包数**：9
**固化备注**：修复 description 弱模型门禁：移除英文 Load when 尾巴 + role-program-mgmt 精简至合规（全局生效）
**最新变更**：v21.6.0 新增项目群/项目集协同层（见下方已完成 0 条）

### 已完成
（无则写「无」）
0. **项目群/项目集协同层（2026-08-16，编排器 v21.6.0）**：新增第 9 角色包 `role-program-mgmt`（v1.0.0）——①`SKILL.md` 路由表 7 环节 `define_program`/`manage_benefits`/`map_dependencies`/`align_schedule`/`standardize_execution`/`review_program`/`close_program` + 闭环执行系统 + 治理铁律/时间一致/标准一致；②`domain/program-management.md` 7 环节主流程 + `program-management__resources/program_details.md` 明细（治理三层模型/四角色/IMS 三层/依赖四类 FS-SS-FF-SF/统一度量口径 CPI-SPI-缺陷密度-里程碑准点率-变更计费率-资源负载/Program Board tranche 决策/收尾）；③新增 `references/program_management.md` 标准（PMI SPM 5th 8 原则 6 绩效域 + MSP + IMS/EVM/PI Planning）+ shared 副本；④台账 27→30（`28_项目群注册.csv`/`29_项目依赖矩阵.csv`/`30_项目群主进度.csv`）+ directory_structure.md + api_contracts.md 登记 7 action + 协作总览补 role-program-mgmt 层；⑤SKILL_INDEX.md 第 8 行 + 使用规则第 15 条 + v21.6.0；⑥编排器 §3 新增「项目群协同」执行模式 + §4 路由表第 8 行 + v21.5.9→v21.6.0；⑦AGENTS.md 8→9 角色包；⑧打包/部署/固化 5 脚本角色清单补 role-program-mgmt；校验双通过（version-consistency 9 包 + descriptions 16 个）；已固化部署。
1. **项目启动治理完善（2026-08-16，编排器 v21.5.9）**：①`role-project-init` 新增 `define_org_structure` 环节（组织架构 + RACI 矩阵：团队构成/角色职责/汇报关系/决策权限/R/A/C/I，`台账/27_组织架构.csv`，v21.2.0）与 `define_issue_escalation` 环节（问题解决与升级机制：P1~P4 分级 + 四级升级阶梯 L1~L4 + 响应时限 + 单一 Owner，`12_风险问题台账.csv` 升级字段）；②`create_charter` 补 SMART 成功标准/PM 任命职权/签字批准（对齐 PMBOK 章程六要素）；③`check_ready` 新增「组织架构已明确」「问题升级机制已确立」两条硬门禁，`init_baseline` 落盘衔接（27 个 CSV）；④role-governance 风险详情补问题升级子流程（P1~P4 表 + 升级阶梯表 + 单一 Owner 原则，project_risk_details.md v1.1.0），governance.md create_baseline 20→27；⑤台账清单 26→27（directory_structure.md + shared 副本 + api_contracts 登记两新 action + create_baseline 27 个）+ SKILL_INDEX.md 第 14 条 + 编排器 v21.5.9；校验双通过（version-consistency + descriptions）；已固化部署。
2. 文档整理批次（2026-08-07）：README_打包说明.md 更新 v21.3.2；TRAE部署与启用指南.md（v8.0.0）归档 docs/legacy/；新增 docs/opencode启用指南.md；历史方案/归档（Token优化与CSV化改造方案_v21.0.0.md、项目归档报告.md）归置 docs/；opencode.json.bak 清理（留痕 OP-AUDIT-002）；deploy 误建 C:/ 目录清理（留痕 OP-AUDIT-003）+ deploy_skills.sh 全局路径平台自适应修复。
3. 嵌套能力模型体系适配（2026-08-10）：team-orchestration（5 文件 64 处）+ multi-perspective-validation（2 文件 33 处）Opus/Sonnet/Haiku → S0~S3 档位；接入编排器 §4.1 + SKILL_INDEX 第 11 条；编排器升 v21.4.0；已部署 4 目标目录。
4. **多项目隔离第 5 层（2026-08-14，v21.5.1）**：`multi_project_isolation.md` 新增「全局环境资产注册与冲突仲裁」——每台服务器建立全局统一环境清单库 `台账/25_环境资源清单.csv`（端口/容器/数据库/大模型等独占资源先注册先得，冲突升阶 change_audit 留痕）；`role-project-init` 新增 `register_env_asset` 环节 + check_ready「资源无未裁决冲突」门禁（v21.0.1/v21.2.2）；`model_selection.md` §7.1 新增本地工具/脚本运行目标缺省=本地轻量模型档；`directory_structure.md`/`api_contracts.md`/`SKILL_INDEX.md`/`common_standards.md`/编排器 SKILL.md 同步；已打包 8 包 + 部署 4 目标目录（含 opencode 全局库）。
5. **CMDB 轻量级资源管理工具 v1.0.0（2026-08-14，v21.5.2）**：新增 `tools/cmdb/cmdb-cli.py`（注册/查询/释放/冲突检测；SQLite 数据库；审计日志；CSV 导出）；更新 `multi_project_isolation.md` 第 10 节补充 CMDB 使用示例；更新 `directory_structure.md` 添加 tools/cmdb/ 目录；更新 `api_contracts.md` 添加 CMDB 工具条目；更新 `SKILL_INDEX.md` 添加 CMDB 工具条目；更新编排器 SKILL.md 版本至 v21.5.2；已部署到 dist/ + 4 目标目录（含 opencode 全局库）。
6. **CMDB 本机资产全面盘点登记（2026-08-14）**：初始化 `tools/cmdb/cmdb.db` 并全面盘点登记本机资源——主机 `gogojajadeMac-mini`（Mac14,3 / Apple M2 / 16GB，IP 脱敏 `xxx.xxx.xxx.xxx`，主机名经授权保留）；资源 8 项：Ollama 模型 qwen2.5-coder:7b、qwen2.5:7b，GPU Apple-M2，端口 11434（Ollama）/8080（llama-server）/5080/5101（Python 本地服务）/4040（ngrok）；全部归属 DevProjectTeamSkill，状态 occupied；导出脱敏快照至 `tools/cmdb/cmdb_export.csv`（Docker 未安装，无数据库/中间件）。
7. **Ollama/llama-server 重复服务收敛（2026-08-14）**：识别 `ollama serve`(11434) 与 `llama-server`(8080) 重复驻留同一模型（llama-server 加载的正是 Ollama blob）；整改方案 A 收敛到 Ollama——停止并移除 llama-server 8080 进程，CMDB 释放 8080 记录，11434 备注标记「本地模型推理统一入口」；修复 Ollama 0.32 模型目录迁移问题（真实模型在 `~/.local/lib/ollama`，新默认 `~/.ollama/models` 为空壳），以 `OLLAMA_MODELS=/Users/gogo/.local/lib/ollama ollama serve` 重启恢复双模型；opencode 全局配置（`~/.config/opencode/opencode.jsonc` 指向 11434）不受影响。
8. **Ollama 改 Ollama.app 自启（2026-08-14）**：清理 `~/.ollama/models` 弃用路径的 18GB partial 下载残片；`~/.ollama/models` 改为符号链接 → `~/.local/lib/ollama`（同一 APFS 卷 disk3s5，零拷贝）；Ollama.app 已加入 macOS 登录项（`System Events make login item`），开机自启后即识别双模型（qwen2.5:7b / qwen2.5-coder:7b），serve 由 App 托管（非手动 nohup），11434 OpenAI 兼容端点验证通过。
9. **Docker Desktop 启用并自启（2026-08-14）**：确认 Docker Desktop 4.85.0 已完整安装于 `~/Applications/Docker.app`（2.1GB，用户级目录，此前未启动故盘点显示"不可用"）；`open ~/Applications/Docker.app` 启动引擎（18s 就绪），Server 29.6.2 / aarch64 / Compose v5.3.1；已加入 macOS 登录项开机自启；CMDB 登记 `container=docker-desktop`（high）。
10. **CMDB 基础软件信息补全（2026-08-14）**：发现软件信息收集不全，扩展 `cmdb-cli.py` 的 register `--type` choices 增加 `software`；全面盘点本机基础软件并登记 30 项——开发工具（Homebrew 6.0.15 + 92 formula + 1 cask、Git 2.50.1、Node v26.3.0、npm 11.16.0、Python 3.9.6/3.12、FFmpeg 8.1.1、CMake 4.3.2、cloudflared、ngrok、Tailscale）、GUI 应用（Docker Desktop、VS Code、OpenCode、TRAE CN、Ollama、Chrome、GitHub Copilot、llama.cpp、Java、Swift/Clang 工具链、WPS、微信、飞书、通义千问、扣子、印象笔记、WhatsApp、AweSun）；导出脱敏快照，累计 38 条资源记录。
11. **本机资产信息提交 GitHub（2026-08-14）**：按 `multi_project_isolation.md` §10 规范生成 `台账/25_环境资源清单.csv`（文档归档版），基于 CMDB 快照 38 条资源，剔除 IP/账号/密钥等敏感信息（主机名经授权保留，IP 脱敏 `xxx.xxx.xxx.xxx`）；提交前做敏感信息扫描确认无真实 IP/密码/令牌；入库 194 文件。

### 进行中
（正在改动、未固化到磁盘的在途工作）
- 本地模型部署批次（2026-08-11）：opencode 全局配置已加入 ollama 本地双模型（qwen2.5:7b、qwen2.5-coder:7b）+ 智谱 GLM-4.7-Flash（apiKey 存 ~/.config/opencode/zhipu-api-key，config 用 {file:} 引用），GLM 接口实测连通；`ollama pull qwen2.5-coder:7b` 后台下载中（约 63%）；台账留痕 OP-AUDIT-004。

### 待办
（下一阶段动作）
1. 本地模型批次：等 qwen2.5-coder:7b 拉取完成后，重启 opencode 验证 3 个 provider 可选可用；若本轮暂不重启，保持后台拉取、下次会话轮询 `ollama list`。
2. 按用户指令推进下一轮技能库维护（skill-authoring 五步流程）或进入具体项目执行模式。
3. 全库禁止新 .xlsx 巡检（当前残留 0）。
4. 交接文档断点区随每次固化自动刷新（solidify.sh §2）。

### 阻塞
（如有风险/阻塞项）
- 无。

### 台账指针
主台账 CSV 路径：台账/（13_安全审计 / 14_授权登记 / 22_阶段复盘）　最近变更号：OP-AUDIT-004

## 2. 关键文件索引

| 文件 | 用途 |
|------|------|
| `AGENTS.md` | 仓库维护规则（核心铁律/命令/GitHub 网络） |
| `.trae/skills/SKILL_INDEX.md` | 8 包路由索引 |
| `.trae/skills/dev-project-team-skill/SKILL.md` | 编排器（v21.5.2，路由/调度/压缩/铁律锚点） |
| `.trae/skills/role-*/SKILL.md` + `domain/*.md` | 7 角色包 |
| `.trae/skills/shared/` | governance/evolution/authoring + references 副本（单源） |
| `.trae/skills/references/` | 公共标准（token_standard / api_contracts / directory_structure / iron_rules / model_selection / environment_standard） |
| `tools/cmdb/` | CMDB 轻量级资源管理工具（cmdb-cli.py + README.md） |
| `tools/` | package/deploy/solidify（.sh+.py 双实现）+ excel_to_csv.py / check_version_consistency.py |
| `docs/opencode启用指南.md` | 当前推荐启用方式（全局/项目级配置） |
| `docs/legacy/TRAE部署与启用指南.md` | v8.0.0 历史 TRAE 部署留档 |
| `README_打包说明.md` | v21.3.2 发布说明 |
| `技能库增强改造方案_v21.3.0.md` | 模型选型/复盘/环境抽取改造方案 |
| `敏捷迭代模式方案_v21.2.1.md` | 敏捷迭代模式方案（+评审报告） |
| `docs/Token优化与CSV化改造方案_v21.0.0.md` | 历史改造方案（v21.0.1 内容，文件名保留） |
| `docs/项目归档报告.md` | v8.0.0 历史归档（2026-07-31） |
| `台账/` | 13_安全审计 / 14_授权登记 / 22_阶段复盘 |
| `Skill_Evolution_Log/`、`Skill_Lessons_Learned/` | CSV 演进台账与经验库 |
| `项目台账模板/` | 业务项目台账模板（MD） |
| `.backup/` | 系统/外部文件操作备份（含时间戳） |
| `.gitignore` | 部署目录/密钥/临时产物隔离 |

## 3. 台账指针

- 安全审计：`台账/13_安全审计台账.csv`（OP-AUDIT-002）
- 授权登记：`台账/14_授权登记.csv`
- 阶段复盘：`台账/22_阶段复盘.csv`
- 演进台账：`Skill_Evolution_Log\01_诊断记录.csv`、`02_哈希基线.csv`
- 经验库：`Skill_Lessons_Learned\01~06_*.csv`

## 4. 约定与铁律
