# 交接文档

## 0. 速览

- **项目目标**：DevProjectTeamSkill 软件研发多角色编排技能库（9 角色包 + 1 编排器）的维护与增强——Token/CSV 化改造、单源共享、敏捷迭代模式、模型选型/阶段复盘/环境抽取、审计授权铁律、目录访问边界、弱模型适配、启动治理完善、项目群/项目集协同层、双套环境拓扑、项目管理咨询层等已全部落地（v21.7.0）。
- **当前阶段**：#11 收口完成；v21.3.x~v21.7.x 增强（模型选型/复盘/环境/铁律卡/审计授权/访问边界/弱模型适配/启动治理/项目群协同/双套环境拓扑/管理咨询层）已固化入 git；最新为 v21.7.0（新增第 9 角色包 role-mgmt-consulting 项目管理咨询）。
- **下一步唯一动作**：按用户指令推进下一轮技能库维护或进入具体项目执行；文档整理批次已固化。

## 1. 工作断点

> 本区由 `tools/solidify.sh` 每次任务完成后自动覆写。
> **新模型/新会话启动，第一步必须先读 `交接文档.md` 全文**，从本区定位上一模型已完成/待办，未读交接文档前禁止读其他项目文档。

- **最近固化时间**：2026-08-27 18:44:56
- **角色包数**：10
- **固化备注**：发布 MCP 服务能力（FastMCP server + publish 自动更新版本 + 各工具接入文档）
- **最新变更**：v21.8.2 评审修复批次——role-architecture description 扩至 150+（D-01）+ 追溯矩阵初始化 11 REQ/5 AE/17 MOD/6 TC（D-02）+ shared/references 漂移归零（D-03）+ lint_repo 白名单(.codebuddy/projects_registry.csv)与评审报告归档 docs/reviews/（D-04）

### 已完成
（无则写「无」）
  - **生产发布机制优化：多工具全局生效（2026-08-27）**：`tools/publish_production.py` 由「仅 opencode 全局库」扩展为「opencode + 已安装工具全局目录」同步发布。新增全局目标矩阵（依据 `references/cross_tool_standard.md`）：TRAE 国际版 `~/.trae/skills/`、TRAE 中国版 `~/.trae-cn/skills/`、WorkBuddy `~/.workbuddy/skills/`、Claude Code `~/.claude/skills/`、Copilot `~/.copilot/skills/`、Agents `~/.agents/skills/`；opencode 用整库重建，其余用精确同步（仅清本仓库发布集子项，保留用户其他全局技能）且兼容 Windows junction/symlink（`os.rmdir` 仅删链接本身）。新增 `--no-extra-globals`/`--extra-globals trae,workbuddy`/`--all-globals` 控制参数，默认自动发现已安装工具。实测：v21.10.0 已同步至 opencode + trae-cn + workbuddy + copilot（原 junction 替换为真实副本，源码单源未污染）；AGENTS.md 生产发布段与铁律#2 同步更新。
  - **opencode 项目迁移路径卡死彻底修复工具化 + 项目迁移 SOP（2026-08-26）**：根因——三个项目自 `D:\trae` 迁至 `D:\MyProjects` 后，opencode 库（`~/.local/share/opencode/opencode.db`）中历史会话消息 cwd 与桌面窗口级状态仍持旧路径，realPath ENOENT 致「发消息无反应」（恢复旧会话与旧窗口新建会话皆中招；审计 OP-AUDIT-044~046）。三层修复：①junction 软链 `D:\trae→D:\MyProjects` 即时兼容全部旧引用（可逆）；②新增跨平台工具 `tools/opencode_path_fix.py`——`--scan` 死亡根目录检测（从 project/project_directory/session 小表收集登记根、磁盘核验、全表统计引用）、`--fix --map "旧=>新"` 三形态替换（正斜杠/单反斜杠/JSON 双反斜杠）、独占锁预检（opencode 运行中拒执行）、写前自动备份、WAL checkpoint、残留复验；③项目迁移标准操作流程：移动/改名后先关 opencode → `python tools/opencode_path_fix.py --scan` 查死路径 → `--fix --map ...` 归位 → 重开；junction 仅作免关闭的兜底手段。**待办深清**（用户侧一次性）：完全退出 opencode 后独立 PowerShell 执行 `--fix --map "D:\trae=>D:\MyProjects" --map "D:\MyProjects\lark-workflow-handbook-deck=>D:\MyProjects\lark-training-ppt-generator"`（第二组为 lark 项目改名残留，scan 实测 1010 条引用，junction 无法覆盖）；备份 `.backup/opencode_data_backup_20260826_202726`。审计 OP-AUDIT-047。
- **项目范围跟踪机制完善（2026-08-25，v21.8.2 后续）**：补全范围跟踪能力（原仅「需求-架构-代码-测试」三方追溯，缺范围基准/覆盖度指标/蔓延缩水检测且 scope-change.md 引用的 06/07 台账从未建立）——①`references/traceability_standard.md` 升 v1.1.0：扩展 RTM（新增 PRIORITY/SCOPE_STATUS/BASELINE_VER/SOURCE/VERIFY_METHOD/CHANGE_REFS）+ Scope Baseline（WBS 分解/范围外登记）+ 覆盖度指标与健康分 + 蔓延/缩水检测 + 变更基线化；②新增 `tools/scope_tracker.py`（init/metrics/gate/change 四子命令，gate 内部复用 check_traceability 做一致性）；③`台账/需求-架构-代码追溯矩阵.csv` 扩列并回填 11 REQ（MoSCoW/状态/基线版本）；④初始化缺失的 `06_范围变更台账.csv`/`07_范围跟踪台账.csv`；⑤`role-governance/domain/scope-change.md` 升 v21.1.0 接入 scope_tracker + 范围基准/WBS/蔓延缩水；⑥AGENTS.md 命令表补 scope_tracker 四条；⑦验证：init/metrics/gate 全通过（健康分 100），change 实测 OK。对齐 PMBOK 范围管理 / IEEE 29148 / MoSCoW / CCB。**待 solidify 固化 + git commit**（本批次为技能源改动，须走固化闭环）。
- **本体全面评审 v21.8.2 缺陷修复批次（2026-08-22，编排器 v21.8.2）**：按五视角+第三方评审（产出 `docs/reviews/评审报告_技能库本体_v21.8.2_*.csv` 三份）并逐项修复：①**D-01** `role-architecture` description 扩至 150+（补「架构评估/技术选型」触发词），descriptions 门禁 18/18 全绿；②**D-02** 初始化 `台账/需求-架构-代码追溯矩阵.csv`（铁律 #10 落地）——补录存量 11 REQ（敏捷启动 6 + IT-02 5）/ 5 AE / 17 MOD / 6 TC，`check_traceability.py` 门禁通过（无孤儿无断链）；③**D-03/F-16** 以 `references/` 为基准重建 `shared/references/` 副本（4 文件 diff 归零 + 补 `traceability_standard.md` 副本，16/16 一致），重跑 deploy 同步三部署目录；④**D-04** 仓库卫生：`lint_repo.py` 白名单加 `.codebuddy/`（IDE 数据目录）+ `projects_registry.csv`（nightly_quality_gate 硬编码依赖），评审报告归档 `docs/reviews/`（21.8.1×3 + 21.8.2×3），清理 `.DS_Store/.pytest_cache/.venv/desensitize_report×2/scan_report×3`，lint 23 error→0；⑤**F-05** 台账/32_镜像同步记录.csv 未提交修改随本轮提交入库；⑥**D-07** SKILL_INDEX/编排器升 v21.8.2 口径一致。校验：version/closure/release_gate/descriptions/traceability/deprecation/lint 七项门禁全绿；dist 打包 + 三目录部署完成。
- **opencode MCP 接入并全局生效（2026-08-20）**：`tools/mcp_server.py`（320 行，暴露 10 工具 solidify/deploy_skills/package_skills/check_* /cmdb/desensitize/list_skills + resources skill://ref:// + prompts route_role/stage_review/skill_authoring）注册为 opencode 本地 MCP：项目 `opencode.json`（相对路径 `tools/mcp_server.py`）+ 全局 `~/.config/opencode/opencode.jsonc`（绝对路径 `/Volumes/BR256G/DevProjectTeamSkill/tools/mcp_server.py`，**任意项目全局生效**，已验证 /tmp 目录 `opencode mcp list` → connected + tools/call list_skills 返回 9 角色包）；`uv run --python 3.10 --with mcp[cli]` 动态拉依赖；`.trae/mcp.json`（TRAE 格式）同步登记；pre-commit B 级钩子豁免 MCP URI scheme（skill:// ref:// 误报）。**注意**：全局 MCP 绝对路径指向本仓库，仓库路径变更需同步全局配置；备份 `.backup/opencode.jsonc.bak.*` + 审计台账 OP-AUDIT-025。提交 5f64d56。
- **工具全局部署（2026-08-20）**：`tools/remove_watermark/` 与 `tools/desensitize/` 完整拷贝至全局技能库 `~/.config/opencode/skills/tools/`（与技能并列的 tools 区），任意项目/全局会话可直接 `python3 ~/.config/opencode/skills/tools/<工具>/<主脚本> <参数>` 调用；全局实例验证通过（remove_watermark --help 正常、desensitize --dictionary 实测替换）。注意：此部署为**手工拷贝**（源码单源仍在 `tools/`，solidify 不部署 tools），后续若两工具更新需手工重拷贝。**本交接文档未自动固化本记录（非技能源改动）**。
- **去水印工具 v1.0.0（2026-08-19）**：新增 `tools/remove_watermark/remove_watermark.py`（通用去水印，Word `.docx`/PPT `.pptx`/Excel `.xlsx` 背景/PDF `/图片/纯文本 6 类处理器——Word XML 级删页眉页脚 `v:textpath` 文字水印+`w:pict` 图片水印、PPT 删形状水印（--text/名称含 Watermark/水印/≥32pt）、Excel 删 sheetPr/picture 背景+--auto 清 legacyDrawingHF、PDF PyMuPDF 关键字 redact+--auto 跨页重复+--rect 相对区域、图片 --rect/--corner/--auto 边缘启发式+--fill blur/edge/white、文本整行印章+行尾短语+--auto 高频重复行）+ `.sh`/`.ps1` 封装 + `README.md`（v1.0.0，含支持矩阵/参数/各格式注意事项/定位：脱敏清理信息泄露、去水印清理视觉痕迹）；Word/PPT/Excel/图片/文本 5 类实测通过（输出文件有效可再打开），PDF 因本机无 PyMuPDF 用 mock 验证三条路径；`parse_xml_tolerant` 容错 r: 前缀未声明文件；SKILL_INDEX 第 18 条 + AGENTS 命令表同步。用法：`python3 tools/remove_watermark/remove_watermark.py <目标> --auto --in-place`。**待固化提交**（与脱敏字典 v1.1.0 同一轮）。
- **文档脱敏字典（2026-08-19，工具 v1.1.0）**：新增 `tools/desensitize/DESENSITIZE_DICTIONARY.md`（脱敏字典文档：规则总览 + 字典列定义 + 维护流程 + 执行流程）+ `tools/desensitize/desensitize_dictionary.csv`（UTF-8 with BOM 关键字集，列 `keyword,level,replacement,type,description`，含 5 个示例占位条目）；`desensitize.py` 新增 `--dictionary` 参数（并入字典关键字生成为 `dictionary_A/B/C` 规则组，跳过表头与 `#` 注释，子串匹配自动 re.escape），支持字典+`--rules`+`--keywords` 三路合并按 A→B→C 执行；README 升级 v1.1.0 + SKILL_INDEX 第 14 条 + AGENTS 命令表同步；实测 5 关键字 4 命中替换验证通过。用法：`python3 tools/desensitize/desensitize.py --dictionary tools/desensitize/desensitize_dictionary.csv <目标> -o <输出>`。
- **best-practice-solution v1.2.0 + 编排器 v21.7.5（2026-08-19，第二轮五视角评审）**：对既有评审流程方案（best-practice-solution v1.1 + MPV 聚合决策）做 FULL 五视角评审（架构/完备性/门禁测试/安全合规/效率），产出 `评审报告_best-practice-solution_v1.1.0_多视角评审.csv`（49 条），并逐项落地：①**LIGHT-P0 纯本地档**（references + recalled_only，0 网络调用，web 条件化，先答后深）——修复"轻量档名不副实"核心矛盾；②**token 预算口径统一**（LIGHT=输出≤2500、webfetch≤1×2000 字符；FULL=20000 含工具 context＝调研6000＋评审6000＋外部核验4000＋收敛4000；超限统一规则入 §3.1，删 research.md 残句）；③**评审三态统一** SIGNED_OFF/CHANGES_REQUESTED/BLOCKED + MPV 决策矩阵引用（不另造三态）；④**触发词路由仲裁**（编排器 §4.1 + SKILL §1.2：技术选型→BPS / ADR 正式化→role-architecture / 代码评审→MPV，无法判定取最严档）；⑤**check_version_consistency.py 扩展扫 SUB_SKILLS + domain 头版本行**；⑥Triage 3→4 问（选型锁定→FULL）+ 黑名单量化（金额≥5 万/许可证/契约破坏/生产核心链路限定）+ 判据冲突取最严档；⑦research.md 独立注入防护/凭据 redacted + SSRF 统一清单（IPv6/link-local/编码混淆/重定向复检，三处一致）；⑧涉密只出不进（register_auth 仅非涉密）；⑨confidence 映射表 + INSUFFICIENT 计数口径 + T3 降级反向信号 + timeliness 字段 + LIGHT 行内引用 + CR 状态回填 + CR 计轮 + 人工签署移到交付后 + closure 模板引用；⑩台账 37 追记 29 条。门禁：version/closure/release/descriptions 四绿。已提交待固化。
- **项目管理咨询层（2026-08-18，编排器 v21.7.0）**：新增第 9 角色包 `role-mgmt-consulting`（v1.0.0）——①`SKILL.md` 路由表 5 环节 `assess_opportunity`/`draft_proposal`/`diagnose_as_is`/`assess_maturity`/`analyze_gap`/`design_solution`/`drive_change`/`coach_org`/`measure_value`/`asset_knowledge` + 闭环执行系统 + 定位铁律（咨询只建议不代决策）/保密铁律（客户数据 A/B 级）；②`domain/consulting-lifecycle.md` 5 环节主流程 + `consulting-lifecycle__resources/maturity_model.md` 明细（自建 5 维框架治理/流程/组织/度量/工具 0~5 级证据必填，对齐 P3M3/CMMI/OPM3 裁剪 + Kotter 8 步/ADKAR）；③新增 `references/consulting_standards.md` 标准（ICM Engagement 生命周期 + 成熟度模型 + 变革管理 + 标准体系映射）+ shared 副本；④台账 33~37（`33_商机管道.csv`/`34_客户登记.csv`/`35_建议书版本.csv`/`36_成熟度基线.csv`/`37_变革计划.csv`）+ `咨询资产/` 目录 + directory_structure.md + api_contracts.md 登记 10 action（§10 + 3 子域）+ 协作总览补 role-mgmt-consulting 层；⑤SKILL_INDEX.md 第 9 行 + 使用规则第 16 条 + v21.7.0；⑥编排器 §3 新增「管理咨询」执行模式 + §4 路由表第 9 行 + v21.6.0→v21.7.0；⑦AGENTS.md 8→9 角色包；⑧打包/部署/固化 6 脚本角色清单补 role-mgmt-consulting；校验：闭环/release_gate/descriptions 三绿（17 SKILL.md），version_consistency 10 包通过（orchestrator「任务契约要点」为固化脚本历史软提示，HEAD 原版即失败，非本次引入）；打包验证 role-mgmt-consulting_v1.0.0.zip（41 文件，含 00_交接文档.md + 内嵌 references/shared）。
- **本体全面评审 + 全部修复（2026-08-18）**：按五维度（P0 工具链自洽/P1 架构一致性/P2 内容质量/P3 行业实践对齐/P4 安全合规）评审全库，产出 `评审报告_技能库本体全面评审_v21.7.0_缺陷.csv`（21 项），并逐项修复：①`tools/check_skill_release_gate.py`/`check_skill_closure.py` 的 ALL_ROLES 补 role-program-mgmt + release_gate 改为中文触发检查（「用户说…时加载」），9 角色门禁全绿；②token_standard v21.1.1 + shared/authoring.md 中文触发标准；③SKILL_INDEX.md 路径修正 + 7 子技能相对引用路径修正（`../../../references/`、`domain/` 前缀）与 SKILL_INDEX 引用溯源，全库字面断链回归验证；④7 子技能（commit-protocol/lsp-ast-integration/multi-perspective-validation/project-memory/self-improve/worktree-isolation/team-orchestration）由 opencc t2s 简体化 + 补闭环执行系统章节 + 版本页脚全角化 + 技能版本元数据（v1.1.0/v1.2.0）；⑤references 4 文档补版本页脚 v21.1.0；⑥shared/references 与主库同步（补 github_access/multi_project_isolation + 同步 4 个差异）；⑦SEC-001 `tools/deploy_skills.py`/`deploy_skills.sh` 硬编码 `C:/Users/gogoj` 脱敏（改 `${USERPROFILE:-$HOME}`/`os.path.expanduser`）；四门禁复验通过（release_gate/closure/descriptions/version_consistency）。**注意**：修复期间曾发生 Python 批量脚本截断 7 子技能 SKILL.md 事故，已 git checkout 恢复后改用可靠方式重做完成，无残留影响。
1. **双套环境拓扑规划（2026-08-17，编排器 v21.7.0）**：新增 `references/environment_topology.md`（v1.1.0，含行业实践评审修订）——每项目「非生产组 dev+test 共享基础软硬件 + 生产组独立隔离」双套拓扑，含文件隔离（目录/数据目录/配置拆分 nonprod/prod）、端口区间隔离（非生产 30000~39999 / 生产独立区间 + CMDB 仲裁）、基础软硬件共用边界（Docker Engine/镜像仓/GPU/工具链共用；服务实例/数据卷必须隔离）、RBAC 权限矩阵（开发者非生产全权+生产只读、QA 执行权、运维唯一生产写权限）、单一真实源（20_环境配置.csv）+ 运维闭环；对齐 AWS 1+N/Hybrid 与 CSA 环境隔离实践；`environment_standard.md` 升级 v21.3.1 衔接环境组/端口区间/共用边界/权限角色列；`20_环境配置.csv` 台账扩展列（directory_structure/api_contracts/common_standards 登记 + shared 副本）；SKILL_INDEX 第 5a 条 + v21.7.0；校验双通过（version-consistency 9 包 + descriptions 16 个）；已固化部署。**v1.1.0 行业实践评审修订（二次评审后）**：+单一制品提升（build once, promote，生产不重构建）+ test 兼作 staging（发布前镜像 prod 验证，衔接金丝雀/蓝绿）+ 版本钉住铁律（镜像 digest/lockfile，禁 latest）+ 端口隔离定位（进程级 vs 网络级强隔离区分）+ 环境即代码 IaC 三态比对（台账↔声明↔实际）防漂移 + 资源配额生产 QoS 保底 + 按需临时环境（生命周期上限）+ 可观测性环境标签 + 一键重置测试数据 + 生产写权限管线化 + break-glass 应急留痕（对齐 HashiCorp/IBM 不可变基础设施 + 12-Factor parity）。
2. **项目群/项目集协同层（2026-08-16，编排器 v21.6.0）**：新增第 9 角色包 `role-program-mgmt`（v1.0.0）——①`SKILL.md` 路由表 7 环节 `define_program`/`manage_benefits`/`map_dependencies`/`align_schedule`/`standardize_execution`/`review_program`/`close_program` + 闭环执行系统 + 治理铁律/时间一致/标准一致；②`domain/program-management.md` 7 环节主流程 + `program-management__resources/program_details.md` 明细（治理三层模型/四角色/IMS 三层/依赖四类 FS-SS-FF-SF/统一度量口径 CPI-SPI-缺陷密度-里程碑准点率-变更计费率-资源负载/Program Board tranche 决策/收尾）；③新增 `references/program_management.md` 标准（PMI SPM 5th 8 原则 6 绩效域 + MSP + IMS/EVM/PI Planning）+ shared 副本；④台账 27→30（`28_项目群注册.csv`/`29_项目依赖矩阵.csv`/`30_项目群主进度.csv`）+ directory_structure.md + api_contracts.md 登记 7 action + 协作总览补 role-program-mgmt 层；⑤SKILL_INDEX.md 第 8 行 + 使用规则第 15 条 + v21.6.0；⑥编排器 §3 新增「项目群协同」执行模式 + §4 路由表第 8 行 + v21.5.9→v21.6.0；⑦AGENTS.md 8→9 角色包；⑧打包/部署/固化 5 脚本角色清单补 role-program-mgmt；校验双通过（version-consistency 9 包 + descriptions 16 个）；已固化部署。
3. **项目启动治理完善（2026-08-16，编排器 v21.5.9）**：①`role-project-init` 新增 `define_org_structure` 环节（组织架构 + RACI 矩阵：团队构成/角色职责/汇报关系/决策权限/R/A/C/I，`台账/27_组织架构.csv`，v21.2.0）与 `define_issue_escalation` 环节（问题解决与升级机制：P1~P4 分级 + 四级升级阶梯 L1~L4 + 响应时限 + 单一 Owner，`12_风险问题台账.csv` 升级字段）；②`create_charter` 补 SMART 成功标准/PM 任命职权/签字批准（对齐 PMBOK 章程六要素）；③`check_ready` 新增「组织架构已明确」「问题升级机制已确立」两条硬门禁，`init_baseline` 落盘衔接（27 个 CSV）；④role-governance 风险详情补问题升级子流程（P1~P4 表 + 升级阶梯表 + 单一 Owner 原则，project_risk_details.md v1.1.0），governance.md create_baseline 20→27；⑤台账清单 26→27（directory_structure.md + shared 副本 + api_contracts 登记两新 action + create_baseline 27 个）+ SKILL_INDEX.md 第 14 条 + 编排器 v21.5.9；校验双通过（version-consistency + descriptions）；已固化部署。
4. 文档整理批次（2026-08-07）：README_打包说明.md 更新 v21.3.2；TRAE部署与启用指南.md（v8.0.0）归档 docs/legacy/；新增 docs/opencode启用指南.md；历史方案/归档（Token优化与CSV化改造方案_v21.0.0.md、项目归档报告.md）归置 docs/；opencode.json.bak 清理（留痕 OP-AUDIT-002）；deploy 误建 C:/ 目录清理（留痕 OP-AUDIT-003）+ deploy_skills.sh 全局路径平台自适应修复。
5. 嵌套能力模型体系适配（2026-08-10）：team-orchestration（5 文件 64 处）+ multi-perspective-validation（2 文件 33 处）Opus/Sonnet/Haiku → S0~S3 档位；接入编排器 §4.1 + SKILL_INDEX 第 11 条；编排器升 v21.4.0；已部署 4 目标目录。
6. **多项目隔离第 5 层（2026-08-14，v21.5.1）**：`multi_project_isolation.md` 新增「全局环境资产注册与冲突仲裁」——每台服务器建立全局统一环境清单库 `台账/25_环境资源清单.csv`（端口/容器/数据库/大模型等独占资源先注册先得，冲突升阶 change_audit 留痕）；`role-project-init` 新增 `register_env_asset` 环节 + check_ready「资源无未裁决冲突」门禁（v21.0.1/v21.2.2）；`model_selection.md` §7.1 新增本地工具/脚本运行目标缺省=本地轻量模型档；`directory_structure.md`/`api_contracts.md`/`SKILL_INDEX.md`/`common_standards.md`/编排器 SKILL.md 同步；已打包 8 包 + 部署 4 目标目录（含 opencode 全局库）。
7. **CMDB 轻量级资源管理工具 v1.0.0（2026-08-14，v21.5.2）**：新增 `tools/cmdb/cmdb-cli.py`（注册/查询/释放/冲突检测；SQLite 数据库；审计日志；CSV 导出）；更新 `multi_project_isolation.md` 第 10 节补充 CMDB 使用示例；更新 `directory_structure.md` 添加 tools/cmdb/ 目录；更新 `api_contracts.md` 添加 CMDB 工具条目；更新 `SKILL_INDEX.md` 添加 CMDB 工具条目；更新编排器 SKILL.md 版本至 v21.5.2；已部署到 dist/ + 4 目标目录（含 opencode 全局库）。
8. **CMDB 本机资产全面盘点登记（2026-08-14）**：初始化 `tools/cmdb/cmdb.db` 并全面盘点登记本机资源——主机 `gogojajadeMac-mini`（Mac14,3 / Apple M2 / 16GB，IP 脱敏 `xxx.xxx.xxx.xxx`，主机名经授权保留）；资源 8 项：Ollama 模型 qwen2.5-coder:7b、qwen2.5:7b，GPU Apple-M2，端口 11434（Ollama）/8080（llama-server）/5080/5101（Python 本地服务）/4040（ngrok）；全部归属 DevProjectTeamSkill，状态 occupied；导出脱敏快照至 `tools/cmdb/cmdb_export.csv`（Docker 未安装，无数据库/中间件）。
9. **Ollama/llama-server 重复服务收敛（2026-08-14）**：识别 `ollama serve`(11434) 与 `llama-server`(8080) 重复驻留同一模型（llama-server 加载的正是 Ollama blob）；整改方案 A 收敛到 Ollama——停止并移除 llama-server 8080 进程，CMDB 释放 8080 记录，11434 备注标记「本地模型推理统一入口」；修复 Ollama 0.32 模型目录迁移问题（真实模型在 `~/.local/lib/ollama`，新默认 `~/.ollama/models` 为空壳），以 `OLLAMA_MODELS=/Users/gogo/.local/lib/ollama ollama serve` 重启恢复双模型；opencode 全局配置（`~/.config/opencode/opencode.jsonc` 指向 11434）不受影响。
10. **Ollama 改 Ollama.app 自启（2026-08-14）**：清理 `~/.ollama/models` 弃用路径的 18GB partial 下载残片；`~/.ollama/models` 改为符号链接 → `~/.local/lib/ollama`（同一 APFS 卷 disk3s5，零拷贝）；Ollama.app 已加入 macOS 登录项（`System Events make login item`），开机自启后即识别双模型（qwen2.5:7b / qwen2.5-coder:7b），serve 由 App 托管（非手动 nohup），11434 OpenAI 兼容端点验证通过。
11. **Docker Desktop 启用并自启（2026-08-14）**：确认 Docker Desktop 4.85.0 已完整安装于 `~/Applications/Docker.app`（2.1GB，用户级目录，此前未启动故盘点显示"不可用"）；`open ~/Applications/Docker.app` 启动引擎（18s 就绪），Server 29.6.2 / aarch64 / Compose v5.3.1；已加入 macOS 登录项开机自启；CMDB 登记 `container=docker-desktop`（high）。
12. **CMDB 基础软件信息补全（2026-08-14）**：发现软件信息收集不全，扩展 `cmdb-cli.py` 的 register `--type` choices 增加 `software`；全面盘点本机基础软件并登记 30 项——开发工具（Homebrew 6.0.15 + 92 formula + 1 cask、Git 2.50.1、Node v26.3.0、npm 11.16.0、Python 3.9.6/3.12、FFmpeg 8.1.1、CMake 4.3.2、cloudflared、ngrok、Tailscale）、GUI 应用（Docker Desktop、VS Code、OpenCode、TRAE CN、Ollama、Chrome、GitHub Copilot、llama.cpp、Java、Swift/Clang 工具链、WPS、微信、飞书、通义千问、扣子、印象笔记、WhatsApp、AweSun）；导出脱敏快照，累计 38 条资源记录。
13. **本机资产信息提交 GitHub（2026-08-14）**：按 `multi_project_isolation.md` §10 规范生成 `台账/25_环境资源清单.csv`（文档归档版），基于 CMDB 快照 38 条资源，剔除 IP/账号/密钥等敏感信息（主机名经授权保留，IP 脱敏 `xxx.xxx.xxx.xxx`）；提交前做敏感信息扫描确认无真实 IP/密码/令牌；入库 194 文件。
14. **仓库目录规整与文件管理能力建设（2026-08-18，v21.7.0）**：①根目录 GBK/散落文件 UTF-8 化归位（README_打包说明.md→README.md、设计文档→docs/、差距台账→台账/）；②台账收敛：program-management/ 28-31 迁入 台账/（单一信息源，删除空 program-management/）；③演进目录 Skill_Evolution_Log/Lessons_Learned 归位 .trae/skills/shared/evolution（kebab 化）；④program-control-ledger → docs/program-control-ledger；⑤新增门面文档 README/CHANGELOG/CONTRIBUTING/REPO_STRUCTURE + 工具 tools/cleanup_workspace.py、tools/lint_repo.py 并接入 .githooks/pre-commit；⑥完善 role-program-mgmt §9.1 单一信息源声明（明确 28-31 驻留 台账/）+ 31_文档配置管理.csv 登记本仓库结构文档(DOC-002)；⑦敏感路径 B 级脱敏（D:\trae / C:/Users/gogoj → 占位）。已提交 02d3bfa + 控制环 AL-013，三端同步。

### 进行中
（正在改动、未固化到磁盘的在途工作）
- 本地模型部署批次（2026-08-11）：opencode 全局配置已加入 ollama 本地双模型（qwen2.5:7b、qwen2.5-coder:7b）+ 智谱 GLM-4.7-Flash（apiKey 存 ~/.config/opencode/zhipu-api-key，config 用 {file:} 引用），GLM 接口实测连通；`ollama pull qwen2.5-coder:7b` 后台下载中（约 63%）；台账留痕 OP-AUDIT-004。

### 待办
（下一阶段动作）
1. ~~本地模型批次~~：不适用当前 Windows 环境（ollama 未安装，原为 macOS 配置）；macOS 侧可轮询 `ollama list` 确认 qwen2.5-coder:7b 拉取状态。
2. 按用户指令推进下一轮技能库维护（skill-authoring 五步流程）或进入具体项目执行模式。
3. ~~全库禁止新 .xlsx 巡检~~：2026-08-27 巡检通过（残留 0）。
4. 交接文档断点区随每次固化自动刷新（solidify.sh §2）。
5. **[可选] 移除 `D:\trae` junction 软链**：opencode 旧路径已彻底归位（2026-08-27），junction 仅为兜底，可安全 `rmdir D:\trae` 移除（属项目外操作，需用户授权）。

### 跨会话交接（2026-08-27，opencode 旧路径无响应根因修复）

**背景**：三项目（DevProjectTeamSkill / Project-management / lark-training-ppt-generator）从 `D:\trae\` 迁至 `D:\MyProjects\` 后，opencode 会话库 `C:\Users\<user>\.local\share\opencode\opencode.db` 仍持旧路径，恢复会话/旧窗口新建会话时 `realPath` ENOENT → `prompt_async failed` → 发消息无反应。

**已完成（全部闭环）**：
- 即时兜底：`D:\trae` → `D:\MyProjects` 软链（junction），三项目旧路径全部解析通过，新对话已可正常使用（OP-AUDIT-046 / 9853af9）。
- 工具化：新增 `tools/opencode_path_fix.py`（死亡根目录扫描 / `--fix --map` 三形态替换 / 独占锁预检 / 写前自动备份 / WAL checkpoint / 残留复验，跨平台），并写入本仓库 SOP（OP-AUDIT-047 / 094cc72）。
- GitHub 同步：`github pull` 已合并远端 4 提交并解决 `交接文档.md` 断点区冲突。
- **[2026-08-27 完成] GitHub 推送**：`github_push.py` 推送成功（PUSH_OK 20.205.243.166 4.8s），`mirror_push.py` 确认双端已同步，HEAD=5431f46 与 origin/main 一致。
- **[2026-08-27 完成] opencode 会话库旧路径彻底归位**：`opencode_path_fix.py --fix` 执行成功——`lark-workflow-handbook-deck→lark-training-ppt-generator` 映射更新 510 行（event.data 491 + part.data 15 + session.directory 2 + session.path 2），残留引用 0 条。备份 `.backup/opencode_db_20260827_114824.db`。`D:\trae` 路径已不在扫描结果中（junction 兜底已覆盖或已自然消解）。junction 可选移除。

### 阻塞
（如有风险/阻塞项）
- 无。

### 台账指针
主台账 CSV 路径：台账/（13_安全审计 / 14_授权登记 / 22_阶段复盘）　最近变更号：OP-AUDIT-004

## 2. 关键文件索引

| 文件 | 用途 |
|------|------|
| `AGENTS.md` | 仓库维护规则（核心铁律/命令/GitHub 网络） |
| `.trae/skills/SKILL_INDEX.md` | 9 包路由索引 |
| `.trae/skills/dev-project-team-skill/SKILL.md` | 编排器（v21.7.0，路由/调度/压缩/铁律锚点 + 管理咨询模式） |
| `.trae/skills/role-*/SKILL.md` + `domain/*.md` | 9 角色包 |
| `.trae/skills/shared/` | governance/evolution/authoring + references 副本（单源） |
| `.trae/skills/references/` | 公共标准（token_standard / api_contracts / directory_structure / iron_rules / model_selection / environment_standard） |
| `tools/cmdb/` | CMDB 轻量级资源管理工具（cmdb-cli.py + README.md） |
| `tools/` | package/deploy/solidify（.sh+.py 双实现）+ excel_to_csv.py / check_version_consistency.py |
| `docs/opencode启用指南.md` | 当前推荐启用方式（全局/项目级配置） |
| `docs/legacy/TRAE部署与启用指南.md` | v8.0.0 历史 TRAE 部署留档 |
| `README.md` | 仓库门面文档（v21.7.0：结构/快速开始/打包部署/文档索引）|
| `技能库增强改造方案_v21.3.0.md` | 模型选型/复盘/环境抽取改造方案 |
| `敏捷迭代模式方案_v21.2.1.md` | 敏捷迭代模式方案（+评审报告） |
| `docs/Token优化与CSV化改造方案_v21.0.0.md` | 历史改造方案（v21.0.1 内容，文件名保留） |
| `docs/项目归档报告.md` | v8.0.0 历史归档（2026-07-31） |
| `台账/` | 13_安全审计 / 14_授权登记 / 22_阶段复盘 |
| `.trae/skills/shared/evolution/skill-evolution-log/`、`skill-lessons-learned/` | CSV 演进台账与经验库（已归位）|
| `项目台账模板/` | 业务项目台账模板（MD） |
| `.backup/` | 系统/外部文件操作备份（含时间戳） |
| `.gitignore` | 部署目录/密钥/临时产物隔离 |

## 3. 台账指针

- 安全审计：`台账/13_安全审计台账.csv`（OP-AUDIT-002）
- 授权登记：`台账/14_授权登记.csv`
- 阶段复盘：`台账/22_阶段复盘.csv`
- 演进台账：`.trae/skills/shared/evolution/skill-evolution-log/01_诊断记录.csv`、`02_哈希基线.csv`
- 经验库：`.trae/skills/shared/evolution/skill-lessons-learned/01~06_*.csv`

## 4. 约定与铁律
