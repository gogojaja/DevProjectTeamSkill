# 评审记录：AI 助手「擅作主张」防控制度与执行闸门（spec mode 独立 Review）

- **评审对象**：
  - R1：`spec.md` + `tasks.md`（`.trae/specs/ai-self-control-20260828/`）
  - R2：`docs/AI助手行为门禁与授权合同.md` + `docs/AI助手行为门禁-执行单模板.csv`（最终交付物）
- **评审模式**：独立只读 Review（评审上下文未参与撰写；禁止写操作）
- **评审日期**：2026-08-28

---

## Review R1（spec/tasks 评审）— 结果：pass（附条件）

### CP 对照（AC-1 ~ AC-6 rule 类）

| CP | 对应 AC | 结论 | 说明 |
|----|---------|------|------|
| CP-1 | AC-1 事件复盘 | pass | E-01~E-05 五事件四列完整，E-01 标红灯越界、E-03/E-04/E-05 标合规，E-04 含「主动停步」 |
| CP-2 | AC-2 六段合同 | pass | 6 段齐；确认段含 ≥4 确认词白名单 +「不接受模糊措辞」 |
| CP-3 | AC-3 工具分级 | pass | 每层 ≥3 真实工具；Tier3 含外部文件写与推送类 |
| CP-4 | AC-4 审批五要素 | pass | 6 列表格 + 示例行（非模糊词） |
| CP-5 | AC-5 参数哈希绑定 | pass | 绑定 3 项 + 重确认 3 步 + 行业出处 |
| CP-6 | AC-6 红黄灯+回滚 | pass | 红灯≥3、黄灯≥2、SOP≥3 步、含「全闸模式」 |

### Rubric 评分

- **AC-7 行业引用覆盖**（阈值 ≥4）：**4.5**。核心条款均有出处；扣分点：部分中文社区链接长期可达性不稳（缺口 G-3）。

### R1 缺口（Actionable Findings，已在 R2 前采纳修正）

- **G-1（高）**：落地点指向不存在的 `user_profile.md`/`项目铁律.md` → 已采纳：唯一锚点改为 AGENTS.md 铁律 #15。
- **G-2（中）**：参数哈希靠 AI 自觉，缺机器闭环 → 已采纳：落地项含 `tools/audit.py --confirm-word/--param-hash` 与台账 13 扩展列。
- **G-3（低）**：外部链接可达性 → 已采纳：出处以「标题+日期」为准，链接可选，不纳入硬门禁。
- **G-4（中）**：与铁律 #7 重叠漂移 → 已采纳：文档头部声明「#7 为前置基础，#15 为强制锚点，两者并行」。

---

## Review R2（最终方案文档评审）— 结果：pass

### CP 对照（AC-1 ~ AC-8）

| CP | 对应 AC | 结论 | 证据 |
|----|---------|------|------|
| CP-1 | AC-1 | pass | 文档 §1 表格 L9-L19：E-01~E-05 五行齐；E-01 含「红灯越界」；E-03/E-04/E-05 含「未越界✅」；E-04 含「主动停步」 |
| CP-2 | AC-2 | pass | §4 六段齐全（理解/拆解/清单/确认/执行/复验）；确认段白名单 5 个词；含「不接受模糊措辞」 |
| CP-3 | AC-3 | pass | §5 Tier1 含 Read/Grep/Glob/LS/SearchCodebase；Tier2 含 Edit/Write；Tier3 含外部文件写/DeleteFile/solidify/publish_production/mirror_push |
| CP-4 | AC-4 | pass | §6 六列表头齐 + 1 行示例（OP-EX01，具体到模型名与数值，无模糊词）；执行单 CSV 表头与之一致 |
| CP-5 | AC-5 | pass | §7 绑定 3 项（操作名/目标路径/SHA-256 前 10 位）；重确认流程含暂停→新清单→旧哈希作废；引用 PlanVault #14 + Zalt 2026 |
| CP-6 | AC-6 | pass | §8 红灯 4 条、黄灯 3 条、SOP 3 步；含「连续 2 次红灯→进入全闸模式」与解除条件 |
| CP-7 | AC-7 | pass | §3 出处表 7 行，每行含标题+日期+原主张+裁剪应用（>6 行阈值） |
| CP-8 | AC-8 | pass | 客观命令证据：`git status --short` 仅 3 个新增文件（.md×2/.csv×1，无 .py/.sh）；代码块语言标记仅 `markdown`；CSV UTF-8 BOM=True；中文字符 2155（<6000 阈值） |

### R2 评审异常记录（如实留痕）

评审期间发现最终文档磁盘内容被外部改写（非评审上下文所为，来源不明），改写中引入两处**事实失真**：

1. §6 示例写成「8 恢复为 32/16、文件 opencode.json」——与 E-01 真实参数（32768→131072、文件 `opencode.jsonc`）不符；
2. §9 标题声称「已落地」——但 AGENTS.md 实际仅有 #1~#14（含 #7a/#7b），**#15 并未写入**，声称已落地构成虚假陈述。

评审意见：两处失真均须修正后方可通过。主会话已修正（§6 示例恢复真实参数与 Tier3 定级；§9 改为「待落地，本轮一律不执行」+ 事实声明）。修正后复核 CP-4/CP-8 均为 pass。

> 该异常本身再次印证了本合同成立的必要性：评审上下文与主会话之间同样存在「未经确认的改写」风险窗口。

### 最终结论

```text
RESULT: pass
- 全部 CP-1~CP-8 pass
- AC-7 rubric 得分 4.5（阈值 4）
- R1 缺口 G-1~G-4 已全部采纳修正并复核
- 无遗留 actionable finding
```

> 评审人：独立 Review 上下文（R1 全文评审 + R2 证据定位）+ 主会话客观命令补充（AC-8 机械检查）
> 附注：git 提交/推送按用户指令「暂不执行」挂起，等待用户显式指令。
