# DevProjectTeamSkill 环境变量配置模板
# 复制为 .env 并根据实际环境修改（所有变量均为可选）
#
# 路径定位优先级：环境变量 > 同级目录约定（../<project>）> 配置文件
# 代理脚本（_hub_proxy.py / _scheduler_proxy.py / _model_router_proxy.py）
# 已实现三级动态解析，通常只需在同级目录放置对应项目即可。

# === 本仓库根目录 ===
# 通常不需要设置，自动检测（tools/ 的上级目录）
# PROJECT_ROOT=/path/to/DevProjectTeamSkill

# === 可选插件路径（独立项目） ===
# 按同级目录约定自动检测（<repo>/../<project>），也可手动指定绝对路径

# dev-git-hub：Git 基建（推送工具单一信源，授权 AUTH-014）
# DEV_GIT_HUB_ROOT=/path/to/dev-git-hub

# dev-task-scheduler：定时任务管理（授权 AUTH-015）
# DEV_TASK_SCHEDULER_ROOT=/path/to/dev-task-scheduler

# dev-model-router：多模型分层编排（授权 AUTH-016）
# DEV_MODEL_ROUTER_ROOT=/path/to/dev-model-router

# dev-project-mgmt：项目管理工具集（授权 AUTH-022）
# DEV_PROJECT_MGMT_ROOT=/path/to/dev-project-mgmt

# dev-security-tools：安全审计工具集（授权 AUTH-023）
# DEV_SECURITY_TOOLS_ROOT=/path/to/dev-security-tools

# dev-test-tools：测试工具集（授权 AUTH-024）
# DEV_TEST_TOOLS_ROOT=/path/to/dev-test-tools

# free-api-hub：API 聚合服务
# FREE_API_HUB_ROOT=/path/to/free-api-hub

# === API 密钥（仅 MCP Server 运行时使用） ===
# DEEPSEEK_API_KEY=your-key-here
# ARK_API_KEY=your-key-here
